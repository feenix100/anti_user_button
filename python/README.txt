Create a new virtual environment, cd and copy the antiuserbutton.py file into the venv. 

Then run in terminal: python antiuserbutton.py

To create a python virtual environment:

python -m buttonEnv venv